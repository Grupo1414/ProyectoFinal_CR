<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/main_container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white"
    android:padding="16dp">

    <!-- Título de la sección de añadir tarjeta -->
    <TextView
        android:id="@+id/textView_add_card"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Añadir una tarjeta de crédito o débito"
        android:textAppearance="@style/TextAppearance.AppCompat.Medium"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="24dp"/>

    <!-- Campos de entrada para los detalles de la tarjeta -->
    <EditText
        android:id="@+id/editText_card_number"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Número de tarjeta"
        android:inputType="numberDecimal"
        android:background="@drawable/edit_text_background"
        android:padding="12dp"
        android:layout_below="@id/textView_add_card"
        android:layout_marginTop="16dp"/>

    <EditText
        android:id="@+id/editText_expiry_date"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Fecha de caducidad MM/AA"
        android:inputType="date"
        android:background="@drawable/edit_text_background"
        android:padding="12dp"
        android:layout_below="@id/editText_card_number"
        android:layout_marginTop="16dp"/>

    <EditText
        android:id="@+id/editText_cvc"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Código de seguridad CVC"
        android:inputType="numberDecimal"
        android:background="@drawable/edit_text_background"
        android:padding="12dp"
        android:layout_below="@id/editText_expiry_date"
        android:layout_marginTop="16dp"/>

    <Spinner
        android:id="@+id/spinner_country"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/spinner_background"
        android:entries="@array/country_array"
        android:layout_below="@id/editText_cvc"
        android:layout_marginTop="16dp"/>

    <EditText
        android:id="@+id/editText_cardholder_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Nombre del titular de la tarjeta"
        android:inputType="textPersonName"
        android:background="@drawable/edit_text_background"
        android:padding="12dp"
        android:layout_below="@id/spinner_country"
        android:layout_marginTop="16dp"/>

    <!-- Botón de acción para añadir la tarjeta -->
    <Button
        android:id="@+id/button_add_card"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Añadir tarjeta"
        android:background="#CDDC39"
        android:textColor="@android:color/white"
        android:layout_below="@id/editText_cardholder_name"
        android:layout_marginTop="24dp"/>

</RelativeLayout>
